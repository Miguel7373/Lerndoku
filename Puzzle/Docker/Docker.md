Softwear running as user

daemon

ausbrechen aus root 

daemon wird root recht enzogen damit verhindert wird das die person die übernimmt keine root rechte hat


ps = process status 
grep mit pipe filert nach such text
 

user id im docker wird aufeien anderen user id geparst so das im docker du root hast und aber der daemon nicht auf deinem system da er dann nur einen user hat



Docker Compose 

